fun main(): int {
  len: int;
  len = 100;
  buf: ^char;
  ^buf = malloc(len + 1);
  val: ^char;
  ^val = ^buf;

  while (^val < ^buf + len) {
    val = 'A';
    ^val = ^val + 1;
  }
  val = 0; // terminate string

  puts(^buf);
  free(^buf);

  return 0;
}
