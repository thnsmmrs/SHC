fun main(): int {
  len: int;
  len = 100;
  buf: ^char;
  ^buf = malloc(len);
  val: ^char;
  ^val = ^buf;
  i: int;
  i = 0;
  while (i < len) {
    ^val = ^val + 1;
    val = 'A';
    i = i + 1;
  }

  puts(^buf);
  free(^buf);

  return 0;
}
